import { PreventCharDirective } from './prevent-char.directive';

describe('PreventCharDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventCharDirective();
    expect(directive).toBeTruthy();
  });
});
